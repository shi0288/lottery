<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mcp.lottery.mapper.UserMapper">



    <update id="addBalance">
        UPDATE USER SET  balance=balance+#{balance}  where id=#{id}
    </update>

    <select id="getAll" resultType="com.mcp.lottery.model.User">
        select a.*,b.is_open as isOpen,b.init_money as initMoney from user a left join user_rule b on a.id=b.uid
    </select>


    <update id="openTouzhu">
        update user_rule set is_open=1 where uid=#{uid}
    </update>

    <update id="closeTouzhu">
        update user_rule set is_open=0 where uid=#{uid}
    </update>

    <update id="updateInitMoney">
        UPDATE user_rule SET  init_money=#{initMoney}  where uid=#{uid}
    </update>






</mapper>